% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{get_bad_ct_vlm}
\alias{get_bad_ct_vlm}
\title{Get bad controlled terminology values for a variable with value level metadata}
\usage{
get_bad_ct_vlm(data, metacore, var, na_acceptable = NULL, .internal = FALSE)
}
\arguments{
\item{data}{Data to check.}

\item{metacore}{A metacore object containing the dataset and value level
metadata specification.}

\item{var}{Name of the variable to check.}

\item{na_acceptable}{Logical scalar indicating whether missing values should be
accepted. If \code{TRUE}, \code{NA} and \code{""} are treated as valid for character
controlled terminology and \code{NA} for non-character controlled terminology.}
}
\value{
A named list containing only the VLM codelists with invalid values.
Each element is named \code{"Codelist: <where_clause>"} and contains the unique
invalid values found for that VLM condition. If no invalid values are found,
an empty list is returned.
}
\description{
Checks a variable against the controlled terminology defined by value level
metadata (VLM) in a metacore specification. For each VLM \code{where} clause
associated with the variable, the function subsets the data, retrieves the
relevant codelist, and returns any values found in the dataset that are not
permitted by that codelist.
}
\details{
The function currently supports only VLM conditions using the \code{EQ} operator.
Any other operator triggers a warning and is skipped.
}
